# 🧪 ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ - ПОЛНОЕ РУКОВОДСТВО

## ✅ МОЖНО ТЕСТИРОВАТЬ ЛОКАЛЬНО!

Вы **НЕ ОБЯЗАНЫ** тестировать на сервере. Можно и нужно тестировать локально на своем компьютере.

---

## 📋 ПОДГОТОВКА ДЛЯ ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ

### 1. Настройте .env для локального тестирования

Создайте `.env` файл с такими настройками:

```env
# Локальный режим
FLASK_ENV=development
SECRET_KEY=ваш_любой_ключ_минимум_32_символа_для_локального_теста
SESSION_COOKIE_SECURE=False
WTF_CSRF_SSL_STRICT=False

# Локальная база данных
DATABASE_URL=sqlite:///instance/app.db

# Email - можно использовать тестовый
MAIL_SERVER=smtp.beget.com
MAIL_PORT=465
MAIL_USE_TLS=False
MAIL_USE_SSL=True
MAIL_USERNAME=orders@mainstreamfs.ru
MAIL_PASSWORD=ваш_пароль
MAIL_DEFAULT_SENDER=orders@mainstreamfs.ru

# CloudPayments - ВАЖНО: используйте ТЕСТОВЫЙ режим!
CLOUDPAYMENTS_PUBLIC_ID=pk_46d0e6977b3b40502eba50d058c5f
CLOUDPAYMENTS_API_SECRET=4b3eaa97656242fa6005369b8646555f
CLOUDPAYMENTS_TEST_MODE=True
CLOUDPAYMENTS_WEBHOOK_URL=http://localhost:5000/api/cloudpayments/webhook

# Telegram Bot - можно не указывать для локального теста
TELEGRAM_BOT_TOKEN=8149993826:AAEsqDj2Bm4-vwS78axw33tcaq7swBgH-QI

# Локальный сервер
PORT=5000
SITE_URL=http://localhost:5000

# Test mode - разрешает платежи без регистрации
TEST_MODE=True
```

### 2. Создайте базу данных локально

```bash
# Удалите старую БД (если есть)
rm -f instance/app.db
rm -f app.db
rm -f app_prod.db
rm -f app_dev.db

# Создайте новую
python create_database_final_v3.py

# Создайте миграцию для индексов
flask db migrate -m "Add indexes for performance"

# Примените миграцию
flask db upgrade
```

### 3. Запустите приложение локально

```bash
# Вариант 1: Простой запуск
python run.py

# Вариант 2: С перезагрузкой при изменениях
export FLASK_DEBUG=True
python run.py
```

Приложение будет доступно по адресу: **http://localhost:5000**

---

## 🧪 ЧТО МОЖНО ТЕСТИТЬ ЛОКАЛЬНО

### ✅ Полностью работает локально:

1. **Вся функциональность приложения**
   - Авторизация и роли
   - Управление пользователями (ADMIN)
   - Управление турнирами (ADMIN)
   - Обработка заказов (OPERATOR)
   - Принятие платежей (MOM)
   - Процесс заказа (CUSTOMER)
   - Корзина и оформление

2. **CloudPayments в тестовом режиме**
   - Тестовые карты работают
   - Webhook можно тестировать через ngrok (см. ниже)
   - Все платежные операции

3. **База данных**
   - SQLite работает отлично локально
   - Все CRUD операции
   - Миграции

4. **Email уведомления**
   - Можно проверить отправку
   - Логи покажут что отправляется

### ⚠️ Ограничения локального тестирования:

1. **Webhook от CloudPayments**
   - CloudPayments не сможет отправить webhook на `localhost`
   - **Решение:** Используйте ngrok для создания публичного URL

2. **Telegram Bot**
   - Webhook для бота не будет работать на localhost
   - **Решение:** Используйте ngrok или тестируйте без бота

3. **HTTPS**
   - Локально обычно нет HTTPS
   - Это нормально для тестирования

---

## 🔧 НАСТРОЙКА NGROK ДЛЯ WEBHOOK (если нужно)

### Если нужно тестировать webhook от CloudPayments:

1. **Установите ngrok:**
   - Скачайте с https://ngrok.com/
   - Или через: `brew install ngrok` / `choco install ngrok`

2. **Запустите ngrok:**
```bash
ngrok http 5000
```

3. **Получите публичный URL:**
   - ngrok покажет что-то вроде: `https://abc123.ngrok.io`
   - Используйте этот URL в настройках CloudPayments webhook

4. **Обновите .env:**
```env
CLOUDPAYMENTS_WEBHOOK_URL=https://abc123.ngrok.io/api/cloudpayments/webhook
```

---

## 📊 ПОШАГОВЫЙ ПЛАН ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ

### ШАГ 1: Базовая проверка (5 минут)
```bash
# 1. Запустите приложение
python run.py

# 2. Откройте браузер
http://localhost:5000

# 3. Проверьте:
# - Главная страница открывается
# - Можно войти (admin@mainstreamfs.ru / password123)
# - Админ-панель доступна
```

### ШАГ 2: Тестирование ролей (30 минут)

**ADMIN:**
- Войдите как admin
- Создайте турнир
- Загрузите XML
- Просмотрите заказы

**OPERATOR:**
- Войдите как operator
- Проверьте дашборд
- Попробуйте взять заказ в работу

**MOM:**
- Войдите как mom
- Проверьте финансовую панель

### ШАГ 3: Тестирование заказа (20 минут)

1. **Создайте заказ:**
   - Откройте главную страницу
   - Выберите турнир
   - Добавьте в корзину
   - Оформите заказ

2. **Оплатите тестовой картой:**
   - Используйте тестовую карту: `4242 4242 4242 4242`
   - Любая будущая дата
   - Любой CVV

3. **Проверьте результат:**
   - Заказ должен появиться в админ-панели
   - Статус должен быть `paid`

### ШАГ 4: Полный цикл обработки (30 минут)

1. **OPERATOR обрабатывает заказ:**
   - Взять заказ в работу
   - Загрузить ссылки
   - Отправить клиенту

2. **MOM принимает деньги:**
   - Просмотреть заказ
   - Принять платеж

3. **Клиент получает ссылки:**
   - Проверить email (или логи)
   - Ссылки доступны

---

## 🔍 ОТЛАДКА ЛОКАЛЬНО

### Просмотр логов SQL запросов:

Добавьте в `config.py` (для DevelopmentConfig):
```python
SQLALCHEMY_ECHO = True
```

Теперь в консоли будут видны все SQL запросы.

### Просмотр ошибок:

Все ошибки выводятся в:
- Консоль (где запущен `python run.py`)
- `logs/app.log` (если настроено)

### Проверка базы данных:

```bash
# Открыть БД SQLite
sqlite3 instance/app.db

# Просмотреть таблицы
.tables

# Посмотреть заказы
SELECT * FROM orders;

# Посмотреть пользователей
SELECT email, role FROM users;
```

---

## 🚀 РАЗВЕРТЫВАНИЕ НА СЕРВЕР (после локального тестирования)

### Когда локальное тестирование завершено:

1. **Подготовьте production .env:**
   - Используйте `CORRECTED.env` (см. файл выше)
   - Установите `FLASK_ENV=production`
   - Установите `SESSION_COOKIE_SECURE=True`
   - Установите `CLOUDPAYMENTS_TEST_MODE=False`

2. **Загрузите код на сервер**

3. **Создайте БД на сервере:**
```bash
python create_database_final_v3.py
flask db upgrade
```

4. **Запустите через gunicorn:**
```bash
gunicorn -c gunicorn_config.py wsgi:app
```

---

## ✅ ЧЕКЛИСТ ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ

- [ ] .env настроен для локального теста
- [ ] `CLOUDPAYMENTS_TEST_MODE=True`
- [ ] `TEST_MODE=True` (для тестирования без регистрации)
- [ ] БД создана локально
- [ ] Приложение запускается без ошибок
- [ ] Все роли работают
- [ ] Процесс заказа работает
- [ ] Тестовая оплата проходит
- [ ] Webhook настроен (через ngrok если нужно)

---

## 💡 СОВЕТЫ

1. **Используйте тестовые данные:**
   - Тестовые карты CloudPayments
   - Тестовые email адреса
   - Не используйте реальные платежи

2. **Логи очень полезны:**
   - Все действия логируются
   - Проверяйте `logs/app.log`

3. **Можно тестировать без интернета:**
   - Большая часть функционала работает без интернета
   - Только CloudPayments и Email требуют интернет

4. **Используйте несколько браузеров:**
   - Один для ADMIN
   - Один для OPERATOR  
   - Один для MOM
   - Один для CUSTOMER (инкогнито)

---

## 🎯 ИТОГ

**ДА, можно и нужно тестировать локально!**

1. ✅ Быстрее
2. ✅ Безопаснее (не ломаете production)
3. ✅ Удобнее отладка
4. ✅ Все работает так же

На сервер загружайте только после полного локального тестирования.

