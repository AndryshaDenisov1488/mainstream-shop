# КРАТКИЙ ОТЧЕТ АУДИТА MAINSTREAM SHOP
## Дата: 2025-11-07

---

## 📊 СТАТИСТИКА

**Всего кода:** 31,474 строк
- Python: 11,868 строк
- HTML: 18,714 строк  
- CSS: 534 строки
- JavaScript: 358 строк

**Найдено проблем:**
- 🔴 Критичных: 4
- 🟡 Важных: 4
- 🟢 Мелких: 4

---

## 🔴 КРИТИЧНЫЕ ПРОБЛЕМЫ (исправить НЕМЕДЛЕННО!)

### 1. Race Condition в webhook CloudPayments
**Файл:** `app/api/cloudpayments_endpoints.py:196-208`

**Проблема:** Вложенная транзакция `with db.session.begin()` вызовет ошибку, т.к. Flask-SQLAlchemy уже создает транзакцию автоматически.

**К чему приводит:**
- ❌ Webhook падает с ошибкой
- ❌ Дублирование платежей
- ❌ Потеря денег клиентов

**Как исправить:**
```python
# БЫЛО:
with db.session.begin():  # ❌
    stmt = select(Payment).where(...).with_for_update()

# ДОЛЖНО БЫТЬ:
stmt = select(Payment).where(...).with_for_update()  # ✅
existing_payment = db.session.scalars(stmt).first()
```

---

### 2. Отсутствие индексов в БД
**Файл:** `app/models.py`

**Проблема:** Поля `Order.payment_expires_at`, `Order.status`, `Payment.cp_transaction_id` не имеют индексов.

**К чему приводит:**
- ❌ Медленные запросы при росте БД
- ❌ Scheduler будет сканировать всю таблицу каждую минуту
- ❌ Webhook будет медленно искать платежи

**Как исправить:**
```python
class Order(db.Model):
    payment_expires_at = db.Column(db.DateTime, index=True)  # ✅
    status = db.Column(db.String(50), index=True)  # ✅

class Payment(db.Model):
    cp_transaction_id = db.Column(db.String(100), unique=True, index=True)  # ✅
```

---

### 3. Ошибка импорта в chat_export
**Файл:** `app/api/chat_export_endpoints.py:124`

**Проблема:** Функция `moscow_now_naive()` используется, но не импортирована в начале файла.

**К чему приводит:**
- ❌ `NameError` при экспорте чата в PDF

**Как исправить:**
```python
# Добавить в начало файла:
from app.utils.datetime_utils import moscow_now_naive  # ✅
```

---

### 4. XXE уязвимость в XML парсере
**Файл:** `app/admin/routes.py:488`

**Проблема:** Используется небезопасный парсер XML.

**К чему приводит:**
- ❌ Атакующий может прочитать локальные файлы сервера
- ❌ Возможна DoS атака

**Как исправить:**
```python
# БЫЛО:
import xml.etree.ElementTree as ET  # ❌
tree = ET.parse(filepath)

# ДОЛЖНО БЫТЬ:
import defusedxml.ElementTree as ET  # ✅
tree = ET.parse(filepath)
```

---

## 🟡 ВАЖНЫЕ ПРОБЛЕМЫ (исправить до деплоя)

### 5. Неправильный порядок валидации в payment_routes
**Файл:** `app/main/payment_routes.py:169-180`

**Проблема:** Проверка "все товары для одного спортсмена" происходит ПОСЛЕ удаления старых заказов.

**К чему приводит:**
- ⚠️ Потеря данных при ошибке валидации

**Как исправить:** Переместить валидацию ДО удаления старых заказов.

---

### 6. Нет timeout в HTTP запросах
**Файл:** `app/utils/cloudpayments.py`

**Проблема:** Все `requests.post()` без параметра `timeout`.

**К чему приводит:**
- ⚠️ Запросы могут висеть бесконечно
- ⚠️ Блокировка worker'ов Gunicorn

**Как исправить:**
```python
response = requests.post(url, json=data, headers=headers, timeout=30)  # ✅
```

---

### 7. Дублирование импорта
**Файл:** `app/api/chat_export_endpoints.py:237, 240`

**Проблема:** Импорт `moscow_now_naive` дублируется.

**Как исправить:** Удалить дублирующийся импорт.

---

### 8. Избыточное логирование
**Файл:** `app/utils/cloudpayments.py:283-290`

**Проблема:** Персональные данные логируются на уровне INFO.

**К чему приводит:**
- ⚠️ Нарушение GDPR/152-ФЗ

**Как исправить:** Уже исправлено (перенесено на DEBUG).

---

## 🟢 МЕЛКИЕ ПРОБЛЕМЫ (можно исправить после деплоя)

9. Неиспользуемые импорты в нескольких файлах
10. Hardcoded значения вместо SystemSetting
11. Нет rate limiting на API
12. Нет проверки на None в некоторых запросах

---

## 🎯 АНАЛИЗ USER JOURNEY

### ✅ ЧТО РАБОТАЕТ ХОРОШО:

**Клиент:**
- Регистрация/вход с валидацией
- Выбор турнира и спортсмена
- Корзина в localStorage
- Оформление заказа
- Оплата через CloudPayments
- Получение ссылок на видео

**Оператор:**
- Просмотр новых заказов
- Взятие в работу
- Загрузка ссылок
- Чат с MOM

**MOM:**
- Подтверждение получения денег
- Отправка ссылок клиенту
- Обработка возвратов

**Админ:**
- Управление пользователями
- Управление турнирами
- Загрузка XML
- Аналитика
- Аудит действий

### ⚠️ ЧТО НУЖНО УЛУЧШИТЬ:

**Клиент:**
- Нет капчи (можно забрутфорсить)
- Нет 2FA
- Корзина не синхронизируется между устройствами

**Оператор:**
- Нет проверки доступности ссылок
- Нет real-time обновлений в чате

**MOM:**
- Возврат только вручную (нет автоматического через CloudPayments API)

**Админ:**
- XXE уязвимость в XML парсере (см. проблему #4)

---

## 📋 ЧЕКЛИСТ ПЕРЕД ДЕПЛОЕМ

### Код (ОБЯЗАТЕЛЬНО):
- [ ] Исправить race condition в webhook (#1)
- [ ] Добавить индексы в модели (#2)
- [ ] Исправить импорт в chat_export (#3)
- [ ] Исправить XXE уязвимость (#4)
- [ ] Добавить timeout в HTTP запросы (#6)
- [ ] Переместить валидацию в payment_routes (#5)

### Конфигурация:
- [ ] Сменить дефолтные пароли (admin123, mom123, operator123)
- [ ] Настроить SSL/HTTPS
- [ ] Проверить .gitignore (не коммитить .env файлы)
- [ ] Установить defusedxml: `pip install defusedxml`

### Безопасность:
- [ ] Добавить rate limiting
- [ ] Настроить CSP
- [ ] Настроить HSTS

### Мониторинг:
- [ ] Настроить Sentry для отслеживания ошибок
- [ ] Настроить алерты
- [ ] Проверить ротацию логов

### Тестирование:
- [ ] Протестировать полный путь клиента
- [ ] Протестировать webhook CloudPayments
- [ ] Протестировать scheduler отмены заказов
- [ ] Протестировать отправку email

---

## 🎓 ИТОГОВАЯ ОЦЕНКА

### Качество кода: **7/10**

**Что хорошо:**
- ✅ Четкая структура проекта
- ✅ Разделение ролей
- ✅ Полный аудит действий
- ✅ Интеграция с CloudPayments
- ✅ Telegram бот
- ✅ Безопасная обработка файлов

**Что нужно улучшить:**
- ❌ Исправить 4 критичные проблемы
- ❌ Добавить тесты
- ❌ Добавить мониторинг
- ⚠️ Добавить rate limiting
- ⚠️ Добавить кеширование

### Рекомендация:
**Проект готов к деплою ПОСЛЕ исправления 4 критичных проблем.**

---

## 🚀 ПЛАН ДЕЙСТВИЙ

1. **СЕЙЧАС (1-2 часа):**
   - Исправить race condition (#1)
   - Добавить индексы (#2)
   - Исправить импорт (#3)
   - Установить defusedxml (#4)

2. **ПЕРЕД ДЕПЛОЕМ (2-3 часа):**
   - Добавить timeout в HTTP (#6)
   - Переместить валидацию (#5)
   - Сменить дефолтные пароли
   - Настроить SSL

3. **ПОСЛЕ ДЕПЛОЯ (1-2 недели):**
   - Добавить тесты
   - Добавить rate limiting
   - Настроить Sentry
   - Добавить кеширование

---

**Полный отчет:** См. файл `FULL_CODE_AUDIT_REPORT_FINAL.md`

**Дата:** 2025-11-07  
**Статус:** Готово к исправлениям

