# 🚀 ПОДГОТОВКА К ТЕСТИРОВАНИЮ - ЧЕКЛИСТ

## ⚠️ ВАЖНЫЕ ИЗМЕНЕНИЯ В КОДЕ

### ✅ Исправлено:
1. **Хардкод секретов удален** - все секреты теперь только в .env
2. **SECRET_KEY** - требует минимум 32 символа или генерируется случайный для dev
3. **Path traversal исправлен** - в загрузке файлов chat
4. **Валидация файлов** - размер и тип проверяются
5. **Индексы добавлены** - для производительности
6. **Eager loading** - для избежания N+1 запросов
7. **Транзакции исправлены** - правильное использование begin/commit
8. **Валидация платежей** - улучшена для capture и refund

### 📝 Нужно сделать вручную:

#### 1. Удалить дубликат models.py из корня
```bash
# Проверьте: если в корне есть models.py (не app/models.py), удалите его
rm models.py  # Только если он дубликат app/models.py
```

#### 2. Проверить payment_routes.py
Файл `app/main/payment_routes.py` все еще используется. Если он не нужен (логика дублируется в `/api/order/create`), можно:
- Оставить как есть (работает)
- ИЛИ удалить и убрать импорт из `app/main/routes.py`

**Рекомендация:** Оставить пока, так как может использоваться в шаблонах.

---

## 📋 ПОШАГОВАЯ ПОДГОТОВКА

### ШАГ 1: Подготовка .env файла

1. **Скопируйте .env.example в .env**
```bash
cp .env.example .env
```

2. **Замените все значения на реальные:**
   - `SECRET_KEY` - сгенерируйте: `python -c "import secrets; print(secrets.token_hex(32))"`
   - `MAIL_PASSWORD` - ваш реальный пароль от email
   - `CLOUDPAYMENTS_API_SECRET` - ваш реальный API secret
   - `TELEGRAM_BOT_TOKEN` - ваш реальный токен бота

3. **Проверьте что НЕТ хардкодов в коде:**
   - Все секреты только в .env
   - В config.py нет fallback значений для секретов

### ШАГ 2: Создание базы данных

#### Вариант A: Первый запуск (новая БД)
```bash
# 1. Создать таблицы и начальные данные
python create_database_final_v3.py

# 2. Создать миграцию для индексов
flask db migrate -m "Add indexes for performance"

# 3. Применить миграцию
flask db upgrade
```

#### Вариант B: Обновление существующей БД
```bash
# 1. Создать миграцию для новых индексов
flask db migrate -m "Add indexes for performance"

# 2. Применить миграцию
flask db upgrade

# 3. Проверить что индексы созданы (SQLite)
sqlite3 instance/app.db ".indexes orders"
sqlite3 instance/app.db ".indexes payments"
```

### ШАГ 3: Проверка структуры БД

**Проверьте индексы:**
```bash
# SQLite
sqlite3 instance/app.db ".schema orders" | grep -i index
sqlite3 instance/app.db ".indexes orders"

# Должны быть индексы на:
# - order_number (unique)
# - generated_order_number (unique)
# - customer_id
# - event_id
# - category_id
# - athlete_id
# - operator_id
# - status
# - created_at
# - payment_intent_id
# - payment_expires_at
```

### ШАГ 4: Тестовые пользователи

После создания БД должны быть созданы:
- **Admin:** `admin@mainstreamfs.ru` / `password123`
- **Operator:** `operator@mainstreamfs.ru` / `password123`
- **Mom:** `mom@mainstreamfs.ru` / `password123`

**⚠️ ВАЖНО:** Измените пароли перед production!

---

## 🧪 БЫСТРЫЙ СТАРТ ДЛЯ ТЕСТИРОВАНИЯ

### 1. Запуск приложения
```bash
# Development
python run.py

# Или с gunicorn
gunicorn -c gunicorn_config.py wsgi:app
```

### 2. Проверка основных URL:
- ✅ `/` - главная страница
- ✅ `/auth/login` - вход
- ✅ `/admin` - панель администратора
- ✅ `/operator` - панель оператора
- ✅ `/mom` - панель мамы
- ✅ `/api/health` - проверка API (если есть)

### 3. Проверка логирования
- Проверьте `logs/app.log` на ошибки
- Проверьте консоль на предупреждения

---

## 🔍 ПРОВЕРКА ГОТОВНОСТИ

### ✅ Перед началом тестирования убедитесь:

- [ ] `.env` файл настроен со всеми секретами
- [ ] База данных создана
- [ ] Миграции применены (индексы созданы)
- [ ] Начальные данные созданы (пользователи, типы видео)
- [ ] Приложение запускается без ошибок
- [ ] Все секреты только в .env (не в коде)
- [ ] Логи не содержат критических ошибок

### ✅ Проверка безопасности:

- [ ] `SECRET_KEY` не хардкод
- [ ] `MAIL_PASSWORD` не хардкод
- [ ] `CLOUDPAYMENTS_API_SECRET` не хардкод
- [ ] `TELEGRAM_BOT_TOKEN` не хардкод
- [ ] В production: `SESSION_COOKIE_SECURE=True`
- [ ] В production: `WTF_CSRF_SSL_STRICT=True`

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После правильной настройки:

1. ✅ Приложение запускается без ошибок
2. ✅ Все маршруты возвращают 200 OK (или правильные коды)
3. ✅ База данных содержит:
   - 3 пользователя (admin, operator, mom)
   - 4 типа видео
   - Системные настройки
4. ✅ Индексы созданы на всех нужных полях
5. ✅ Нет предупреждений о секретах в логах
6. ✅ Email настройки работают (можно проверить отправкой тестового письма)

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "SECRET_KEY not set"
**Решение:** Добавьте в .env:
```env
SECRET_KEY=ваш_ключ_минимум_32_символа
```

### Проблема: "MAIL_PASSWORD required"
**Решение:** Добавьте в .env реальный пароль от email

### Проблема: "CloudPayments credentials not set"
**Решение:** Добавьте в .env:
```env
CLOUDPAYMENTS_PUBLIC_ID=ваш_id
CLOUDPAYMENTS_API_SECRET=ваш_secret
```

### Проблема: Индексы не созданы
**Решение:**
```bash
flask db migrate -m "Add indexes"
flask db upgrade
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

После подготовки окружения:

1. ✅ Прочитайте `TESTING_GUIDE.md` - полное руководство по тестированию
2. ✅ Следуйте плану тестирования по этапам
3. ✅ Используйте чеклист из `TESTING_GUIDE.md`
4. ✅ Фиксируйте найденные проблемы

---

## 📞 ВОПРОСЫ?

Если возникли вопросы по подготовке:
1. Проверьте логи приложения
2. Проверьте логи миграций
3. Проверьте что все переменные окружения установлены
4. Убедитесь что база данных существует и доступна

