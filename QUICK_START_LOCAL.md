# 🚀 БЫСТРЫЙ СТАРТ ДЛЯ ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ

## ✅ ВЫ МОЖЕТЕ ТЕСТИРОВАТЬ ВСЁ ЛОКАЛЬНО!

**Да, разработчики тестируют локально, без развертывания на сервере!**

---

## 📋 ДВА ВАРИАНТА ТЕСТИРОВАНИЯ

### Вариант 1: БЕЗ платежей (проще, для начала)

Протестируйте весь функционал кроме реальных платежей.

### Вариант 2: С платежами (полное тестирование)

Полное тестирование включая CloudPayments виджет.

---

## 🎯 ВАРИАНТ 1: БЕЗ ПЛАТЕЖЕЙ (РЕКОМЕНДУЕТСЯ ДЛЯ НАЧАЛА)

### Шаг 1: Убедитесь что БД создана

```powershell
# Проверьте что есть instance/app.db
Test-Path instance\app.db
```

Если нет - создайте:
```powershell
$env:SKIP_SCHEDULER="1"
python create_database_final_v3.py
```

### Шаг 2: Создайте .env (или не создавайте вообще!)

**Можете вообще не создавать .env** - приложение будет работать, просто платежи не будут доступны.

Или создайте минимальный `.env`:

```env
FLASK_ENV=development
DATABASE_URL=sqlite:///instance/app.db
TEST_MODE=True
```

**Не указывайте CLOUDPAYMENTS_* - тогда платежи просто будут показывать понятную ошибку.**

### Шаг 3: Запустите приложение

```powershell
python run.py
```

### Шаг 4: Тестируйте всё кроме платежей

✅ **Работает:**
- Авторизация (admin@mainstreamfs.ru / password123)
- Управление турнирами, категориями, спортсменами
- Создание заказов
- Работа оператора (обработка заказов)
- Работа MOM (принятие платежей)
- Все административные функции

⚠️ **Не работает:**
- Кнопка "Оплатить" (покажет понятную ошибку)

---

## 💳 ВАРИАНТ 2: С ПЛАТЕЖАМИ (ПОЛНОЕ ТЕСТИРОВАНИЕ)

### Шаг 1: Получите тестовые ключи CloudPayments

1. Зарегистрируйтесь в CloudPayments: https://cloudpayments.ru/
2. Зайдите в личный кабинет
3. Найдите раздел "Тестовый режим" или "API ключи"
4. Скопируйте:
   - `Public ID` (начинается с `pk_test_`)
   - `API Secret`

### Шаг 2: Создайте .env файл

```powershell
# Скопируйте шаблон
Copy-Item TEST_ENV_LOCAL.env .env

# Отредактируйте .env и вставьте свои ключи:
notepad .env
```

В `.env` укажите:

```env
FLASK_ENV=development
DATABASE_URL=sqlite:///instance/app.db

# CloudPayments ТЕСТОВЫЕ ключи
CLOUDPAYMENTS_PUBLIC_ID=pk_test_ваш_ключ_сюда
CLOUDPAYMENTS_API_SECRET=ваш_секрет_сюда
CLOUDPAYMENTS_TEST_MODE=True
CLOUDPAYMENTS_WEBHOOK_URL=http://localhost:5000/api/cloudpayments/webhook

TEST_MODE=True
```

### Шаг 3: Перезапустите приложение

```powershell
python run.py
```

### Шаг 4: Тестируйте платежи

1. Создайте заказ
2. Нажмите "Оплатить"
3. Используйте тестовую карту: `4111 1111 1111 1111`
4. CVV: любая (например `123`)
5. Дата: любая будущая (например `12/25`)

✅ **Виджет откроется и примет тестовый платеж!**

---

## 🔧 ИСПРАВЛЕНИЕ ОШИБКИ "HTTP ошибка при оплате"

Если при нажатии "Оплатить" видите ошибку:

### Проблема 1: CloudPayments не настроен

**Симптом:** "CloudPayments не настроен..."

**Решение:**
1. Создайте `.env` файл
2. Добавьте тестовые ключи CloudPayments (см. Вариант 2 выше)
3. Перезапустите приложение

### Проблема 2: Неправильный статус заказа

**Симптом:** "Order is not in checkout_initiated status"

**Решение:**
- Создайте новый заказ через интерфейс
- Убедитесь что нажимаете "Оплатить" сразу после создания

### Проблема 3: Ключи неправильные

**Симптом:** Виджет не открывается или ошибка в консоли браузера

**Решение:**
- Проверьте что `CLOUDPAYMENTS_PUBLIC_ID` начинается с `pk_test_`
- Проверьте что ключи скопированы правильно (без пробелов)
- Перезапустите приложение после изменения .env

---

## 📊 ЧТО МОЖНО ТЕСТИТЬ ЛОКАЛЬНО (БЕЗ СЕРВЕРА)

### ✅ Полностью работает:

1. **Вся административная панель**
   - Управление пользователями
   - Управление турнирами
   - Управление категориями и спортсменами
   - Просмотр заказов
   - Финансовая отчетность

2. **Процесс заказа**
   - Выбор турнира
   - Выбор категории/спортсмена
   - Выбор типа видео
   - Корзина
   - Оформление заказа

3. **Работа оператора**
   - Просмотр новых заказов
   - Принятие заказа в работу
   - Загрузка ссылок на видео
   - Комментарии к заказу

4. **Работа MOM**
   - Просмотр заказов
   - Подтверждение платежей
   - Возвраты
   - Финансовые отчеты

5. **Платежи (если настроены ключи)**
   - Виджет оплаты
   - Тестовые платежи
   - Webhook (требует ngrok, см. ниже)

### ⚠️ Требует настройки:

1. **Webhook от CloudPayments**
   - Нужен публичный URL (используйте ngrok)
   - Без webhook статусы обновляются вручную или через админку

2. **Email уведомления**
   - Нужны SMTP настройки в .env
   - Или можно тестировать без них

---

## 🚀 ДОПОЛНИТЕЛЬНО: WEBHOOK (если нужно)

Если хотите тестировать автоматические обновления статусов через webhook:

1. **Установите ngrok:**
   ```powershell
   # Скачайте с https://ngrok.com/download
   # Или через Chocolatey:
   choco install ngrok
   ```

2. **Запустите ngrok:**
   ```powershell
   ngrok http 5000
   ```

3. **Скопируйте URL** (например: `https://abc123.ngrok-free.app`)

4. **Обновите .env:**
   ```env
   CLOUDPAYMENTS_WEBHOOK_URL=https://abc123.ngrok-free.app/api/cloudpayments/webhook
   ```

5. **Настройте в CloudPayments:**
   - Зайдите в личный кабинет CloudPayments
   - Укажите webhook URL: `https://abc123.ngrok-free.app/api/cloudpayments/webhook`

---

## ✅ ИТОГО

**Вы можете тестировать всё локально!**

1. **Для начала:** Не создавайте .env или создайте минимальный - тестируйте без платежей
2. **Для полного теста:** Добавьте тестовые ключи CloudPayments в .env
3. **Для webhook:** Используйте ngrok

**Разработчики так и делают - всё тестируют локально перед деплоем на сервер!**

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

1. Проверьте консоль браузера (F12) - там будут детальные ошибки
2. Проверьте логи приложения в терминале
3. Убедитесь что БД создана
4. Убедитесь что .env правильно настроен (если используете)

